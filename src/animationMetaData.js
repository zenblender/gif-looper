class AnimationMetaData {
  
  constructor(durationMs, width, height) {
    this.durationMs = durationMs
    this.width      = width
    this.height     = height
  }

}

export default AnimationMetaData
